mixin forgotForm()
    form.form(action="/account/forgot" method="POST")
        h2 I forgot my password! 
        label(for="email" name="email")
        input(type="email" name="email")
        input.button.submit-btn(type="submit" value="Send a Reset")